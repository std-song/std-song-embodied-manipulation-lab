# SO101语言条件操作：训练、执行与D1数据修正

截至2026-09-23，最近确认状态：D0训练与部署链完成，真机自主抓取尚未成功，D1配置准备完成。

## 任务和方法

一个方块、白色杯子、红色瓶盖同时在场。指令为：

1. `Pick up the cube and place it in the white cup.`
2. `Pick up the cube and place it in the red bottle cap.`

L1白杯左/红盖右，L2位置交换，以俯视相机为准。训练同时覆盖两布局和两指令，避免任务与固定目标位置绑定。红盖确实是瓶盖，不以“红杯”标注。颜色和形状同时不同，因此实验不能单独证明纯颜色理解。

双相机观察、实测关节状态和语言进入SmolVLA；输出关节目标动作块。前五维是绝对关节角度，夹爪是校准后的0–100表示，不是六维笛卡尔位姿，也不是动作增量。相机overhead映射camera1，front_left映射camera2。

```text
遥操作 → episode数据与质量检查 → 训练集统计量 → SmolVLA微调
                                            ↓
失败记录 ← 真机反馈与受限控制 ← 反归一化 ← 云端动作预测
    ↓
数据协议修正 → 新数据版本 → 独立开发评估
```

当前训练属于模仿学习，不设置PPO式环境奖励。Flow Matching损失、任务成功判据和RL奖励是不同概念。

## 数据与训练结果

| 数据 | episode数 | 帧数 | 使用方式 |
|---|---:|---:|---|
| 初始pilot | 6 | 4,229 | 采集链检查，不进入D0正式训练 |
| D0 train | 60 | 40,516 | 模型更新与训练归一化统计 |
| D0 dev | 12 | 7,900 | 独立采集；开发评估，不用于梯度更新 |
| D1 train | 计划60 | 未采集确认 | 首批3条检查后计入总数 |
| D1 dev | 计划12 | 未采集确认 | 另行采集，不从训练帧切分 |

SmolVLA与SmolVLM本地离线加载完成。配置冻结视觉编码器、主要更新动作专家并训练state projection；可训练参数99,880,992。单batch反向检查显示155个参数张量有梯度，梯度元素数与可训练参数总数一致，无NaN/Inf。该检查表明参与反向传播，不代表每个元素都获得非零梯度。

动作chunk长度50，内部max_state_dim/max_action_dim为32；真实机器人动作维度6。训练需明确真实维度与episode末尾padding的损失处理，不能把32维内部表示都解释成真实电机。

| 实验 | batch | 更新步数 | 中位秒/步 | 样本/秒约值 | 峰值allocated GiB |
|---|---:|---:|---:|---:|---:|
| smoke | 1 | 100 | 0.1142 | 8.76 | 1.68 |
| smoke | 8 | 200 | 0.5009 | 15.97 | 2.96 |
| smoke | 16 | 100 | 1.0991 | 14.56 | 4.62 |
| smoke | 32 | 100 | 2.1952 | 14.58 | 7.87 |

batch8在这些运行中吞吐最好，所以正式基线采用8。不同实验的更新步数和总样本数不同，不能用其结束loss直接比较batch的统计效率，也不能仅因显存未用满就判定应增大batch。

| checkpoint | 阶段抽样固定噪声dev loss | 含义 |
|---|---:|---|
| 正式基线更新前 | 0.581189 | 当前评估协议下的起点 |
| D0 5k | 0.113043 | 首次正式基线 |
| 续训9k | 0.112139 | 续训候选中最低dev loss |
| 续训10k | 0.112413 | 最终续训checkpoint |

续训是5k之后以1e-5→1e-6余弦学习率延长，不等同从零制定的10k训练。9k相对5k loss下降约0.80%，没有证据表明真机成功率因此提高。保存/重载动作最大差为0。dev指标是固定噪声的阶段抽样结果，不是完整dev损失，也不是机器人成功率。

## 真机证据与失败分析

离线动作检查、录制观测SSH回放、JPEG传输以及实时双相机/状态只读推理均已完成。只读阶段机械臂不运动是设计行为。之后完成受限前缀运动与带夹爪的同步任务。

| 尝试 | 完成chunk | 发送目标 | 响应拒绝 | 耗时 | 人工结果 |
|---|---:|---:|---:|---:|---|
| sync_task_white_003 | 60 | 960 | 0 | 约109.71秒 | 抓取失败 |
| sync_task_white_004 | 60 | 960 | 0 | 约108.45秒 | 抓取失败 |

上述是开发尝试，不作为正式成功率分母。两次使用D0 5k模型；每次预测50步、实际执行前16步，再停止并获取新观察。此模式有网络等待和保持时间，执行节奏不同于30Hz连续演示。原计划的30秒正式评测尚未开展，不能拿这些约110秒开发运行声称符合30秒任务标准。

已发现的事实：

- D0中60/60条起点的elbow_flex贴近上标定边界；57/60条shoulder_lift起点距下边界不足5度。肩关节不是贴近上界。
- D0共9,372/40,516帧肘动作标签按follower标定映射后超范围，比例23.13%（超过半刻度容差）。标签来自遥操作目标，不能据此认定实物发生碰撞或所有这些帧应删除。
- 003中肩/肘约85%命中6刻度单步限制；预测目标与发送目标存在明显差距。数据重录不能自动解除此执行偏差。
- 原生遥操作可以正常跟随，微小24刻度探测失败不能单独证明舵机损坏。原生初始化与早期自定义控制的Acceleration设置不同，因果尚未确证。
- 用户后续补充相机/桌面可能有轻微变化。腕部支架明确是在最近失败测试之后拆下，因此不能解释之前失败。

目前没有单一根因已被充分证明。定位路径是分开核对输入场景、动作映射、演示可执行性与控制响应，而不是只增加训练步数。

## D1决策与完成标准

固定拆支架后的形态，拍摄两布局参考；起点肩肘距标定两端至少约10度，其余关节不顶限位。不为了通过检查扩大标定范围。

训练划分：L1白15、L1红15、L2白15、L2红15。第一组先3条检查，然后补12条；其余各15。另采独立dev12，四种条件各3。硬件、相机、标定和数据版本一并记录。

首轮D1从原SmolVLA预训练权重单独微调，重新计算训练统计量并核对部署动作包络。D0保留作为历史基线；由于场景/起点/硬件形态同时变化，D0与D1端到端结果不能当作严格单因素数据消融。

完成证据：至少能真实完整执行任务；冻结评估条件并报告成功数/总数；保存失败与人工接管；完成一个合理对照；本人能够独立解释数据、损失、采样和控制关键代码。没有预设或虚构成功率。

## 可用于技术交流的当前表述

“搭建SO101双相机语言条件操作实验，完成60条示范的SmolVLA微调、离线保存重载与云端到真机同步执行；通过动作标签和预测/命令/反馈分析，定位到边界起点及控制限速等潜在偏差，正在验证修正数据协议。”

当前不能写“稳定自主抓取”“泛化成功率提升”“真实机器人RL后训练”。代码有AI协助，个人贡献应以能独立解释、修改和验证的部分为准。
